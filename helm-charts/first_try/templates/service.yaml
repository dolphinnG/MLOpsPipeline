apiVersion: v1
kind: Service
metadata:
  name: {{ include "first_try.fullname" . }}
  labels:
    {{- include "first_try.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "first_try.selectorLabels" . | nindent 4 }}
