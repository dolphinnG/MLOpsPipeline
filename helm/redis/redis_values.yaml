fullnameOverride: redis-dolphin-service

architecture: standalone

auth: 
  enabled: true 
  password: redis_password

master: 
  persistence:
    enabled: false
  
tls:  
  enabled: true 
  autoGenerated: false
  existingSecret: redis-tls-secret
  certFilename: "tls.crt"
  certKeyFilename: "tls.key"
  certCAFilename: "ca.crt"
  authClients: false